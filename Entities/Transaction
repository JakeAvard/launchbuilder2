{
  "name": "Transaction",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string",
      "description": "Organization receiving donation"
    },
    "campaign_id": {
      "type": "string",
      "description": "Optional campaign ID if donation is for specific campaign"
    },
    "amount": {
      "type": "number",
      "description": "Donation amount"
    },
    "transaction_date": {
      "type": "string",
      "format": "date",
      "description": "Date of transaction"
    },
    "payment_method": {
      "type": "string",
      "enum": [
        "Stripe",
        "PayPal",
        "Plaid",
        "Manual"
      ],
      "description": "Payment method used"
    },
    "is_recurring": {
      "type": "boolean",
      "default": false,
      "description": "Whether this is a recurring donation"
    },
    "frequency": {
      "type": "string",
      "enum": [
        "one-time",
        "weekly",
        "monthly",
        "quarterly",
        "annually"
      ],
      "default": "one-time",
      "description": "Donation frequency"
    },
    "status": {
      "type": "string",
      "enum": [
        "completed",
        "pending",
        "failed"
      ],
      "default": "completed",
      "description": "Transaction status"
    },
    "notes": {
      "type": "string",
      "description": "Optional notes about the donation"
    }
  },
  "required": [
    "organization_id",
    "amount",
    "transaction_date"
  ]
}
